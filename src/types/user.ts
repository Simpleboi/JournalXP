export interface UserData {
  username: string;
  level: number;
  streak: number;
  points: number;
  rank: string;
  nextRank: string;
  pointsToNextRank: number;
  levelProgress: number;
  recentAchievement: string;
  joinDate: string;
  uid: string;
  inventory: string[],
}
